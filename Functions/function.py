
# Defining a function with def keyword and function name with parameters
def area(length, breadth):

    return (length*breadth) #returning the output

# Storing the value in a variable
result = area(10,5)

#Printing the required value
print("Area of rectangle is :", result)